import Wallet from "@/src/screens/dashboard/online/wallet/Wallet";
import { FC } from "react";

interface walletTabProps {}

const walletTab: FC<walletTabProps> = ({}): JSX.Element => {
	return <Wallet />;
};

export default walletTab;
