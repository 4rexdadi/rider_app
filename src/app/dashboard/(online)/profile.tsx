import Profile from "@/src/screens/dashboard/online/profile/Profile";
import { FC } from "react";

interface profileTabProps {}

const profileTab: FC<profileTabProps> = ({}): JSX.Element => {
	return <Profile />;
};

export default profileTab;
