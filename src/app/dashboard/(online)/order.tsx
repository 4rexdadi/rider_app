import Order from "@/src/screens/dashboard/online/order/Order";
import { FC } from "react";

interface orderTabProps {}

const orderTab: FC<orderTabProps> = ({}): JSX.Element => {
	return <Order />;
};

export default orderTab;
