import Home from "@/src/screens/dashboard/online/home/Home";
import { FC } from "react";

interface homeTabProps {}

const homeTab: FC<homeTabProps> = ({}): JSX.Element => {
	return <Home />;
};

export default homeTab;
