import Offline from "@/src/screens/dashboard/offline/Offline";
import { FC } from "react";

interface offlinePageProps {}

const offlinePage: FC<offlinePageProps> = ({}): JSX.Element => {
	return <Offline />;
};

export default offlinePage;
